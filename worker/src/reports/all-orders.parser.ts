/**
 * SHIM — code thật ở web/src/lib/worker/reports/all-orders.parser.ts.
 *
 * Module 4 (đơn hàng) được chuyển vào gói `web/` ngày 16/09/2026 để Vercel Cron
 * chạy được (Root Directory = web). Sửa production ở file trong `web/`, KHÔNG sửa ở đây.
 */
export * from "../../../web/src/lib/worker/reports/all-orders.parser.ts";
