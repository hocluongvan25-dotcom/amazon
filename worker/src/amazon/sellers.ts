/**
 * Sellers client v1 (tạm thời đọc shop list từ DB; sau này sẽ gọi getMarketplaceParticipations
 * để verify credential).
 */
import type { ActiveShop } from "../db/adapter.ts";

export type { ActiveShop };
